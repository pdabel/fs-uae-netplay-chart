apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fs-uae-netplay.fullname" . }}-test-connection"
  labels:
    {{- include "fs-uae-netplay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fs-uae-netplay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
